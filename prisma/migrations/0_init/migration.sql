-- This is an empty migration.

-- The database tables are expected to already exist, as they are created or imported by the user  due to the specific freeradius schema.
-- The only other solution would be to provide a database schema and configure this schema in freeradius, which is probably more work, as freeradius installations are expected to be already installed. If not, this would a solution.